<% @sets.each do |set| %>
  <p><%= link_to image_tag(set.primary_photo.square.url), set_url(set.id) %></p>
  <p><%= link_to 'View on Flickr.com', set.url %></p>
  <p><strong><%= h set.title %></strong></p>
  <p><%= h set.description %></p>
<% end %>